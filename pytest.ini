[pytest]
pythonpath = .
testpaths = server/tests

# Default test output
addopts = -v --tb=short

# Test categories
markers =
    unit: fast isolated unit tests
    integration: tests involving multiple application components or the database
